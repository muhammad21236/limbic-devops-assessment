# Environment Variables for Docker Compose
# Copy this file to .env and fill in your values

# Application Settings
NODE_ENV=production
FLASK_ENV=production
LOG_LEVEL=info

# Network Settings
APP1_PORT=3000
APP2_PORT=5000

# Internal Service URLs (Docker DNS)
APP2_URL=http://app2:5000

# Cloudflare Tunnel (if storing token here)
# CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# Optional: Database credentials (if adding database later)
# DATABASE_URL=postgresql://user:password@db:5432/limbic
# REDIS_URL=redis://redis:6379/0

# Security
# Generate with: openssl rand -hex 32
SECRET_KEY=change_this_to_a_random_secret_key_in_production

# Optional: Monitoring
# SENTRY_DSN=https://your-sentry-dsn
# NEW_RELIC_LICENSE_KEY=your_key_here
